/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'res.cloudinary.com', 
      'praveera.s3.eu-north-1.amazonaws.com'
    ],
  },
};

export default nextConfig;
